[project]
name = "test_nb"
version = "0.0.1"
description = "test nonebot"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"
